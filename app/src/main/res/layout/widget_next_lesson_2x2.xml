<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/widget_root_layout_2x2"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="0dp"
    android:background="@android:color/transparent"> <!-- Фон теперь прозрачный -->

    <LinearLayout
        android:id="@+id/widget_content_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="@android:color/transparent"> <!-- УДАЛЕНА ССЫЛКА НА widget_background_shape -->

        <FrameLayout
            android:id="@+id/widget_classroom_container_2x2"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="center"
            android:padding="8dp"
            android:background="@drawable/widget_rounded_bg_default"
            tools:background="@drawable/widget_rounded_bg_lecture">

            <TextView
                android:id="@+id/widget_tv_classroom_2x2"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:gravity="center"
                android:textColor="@android:color/white"
                android:textSize="24sp"
                android:textStyle="bold"
                tools:text="К-105" />
        </FrameLayout>

        <TextView
            android:id="@+id/widget_tv_message_2x2"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="@string/widget_loading"
            android:textAppearance="?android:attr/textAppearanceMedium"
            android:textColor="?android:attr/textColorPrimary"
            android:visibility="gone"
            tools:visibility="visible"
            tools:text="Нет занятий" />
    </LinearLayout>

</FrameLayout>
